//Single line comment
/Not a comment
Multi line comment
//Single line comment \
not escaped by newline escape

/* multi line comment start
 * more multi line comment
 */

/* multiline comment on one line */

valid code /* multiline comment after code */

also code /* multiline comment
that is pretty stupid but
I guess you should be allowed to do this */
/*/ //that first bit isn't a comment
/**/ // both of these are comments 
the next line is a comment
//
This is code

here is a \
    single escaped line of code //followed by a comment

fun sum(a) -> foldL(a, 0, (+))
fun max(a*) -> foldL(a, head(a), (x,y) -> if x > y then x else y)
fun min(a*) -> foldL(a, head(a), (x,y) -> if x < y then x else y)

